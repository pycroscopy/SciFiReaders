[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "SciFiReaders"
dynamic = ["version"]
description = "Tools for extracting data and metadata from scientific data files"
readme = "README.rst"
license = { text = "MIT" }
authors = [
    { name = "Pycroscopy contributors", email = "pycroscopy@gmail.com" }
]
keywords = ["imaging", "spectra", "multidimensional", "scientific"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Scientific/Engineering :: Information Analysis"
]
requires-python = ">=3.10"
dependencies = [
    "setuptools",
    "numpy",
    "toolz",
    "cytoolz",
    "dask>=2.20.0",
    "sidpy>=0.11.2",
    "numba",
    "ipython>=7.1.0",
    "pyUSID",
    "gdown",
    "mrcfile",
    "pycroscopy-gwyfile",
    "scipy >= 1.11"
]

[project.optional-dependencies]
hyperspy = ["hyperspy"]
igor2 = ["igor2"]
sid = ["pyUSID", "pyNSID"]
image = ["pillow", "tifffile"]
mcp = ["mcp"]
test = ["pytest", "hyperspy", "pyUSID", "gdown", "aicspylibczi"]

[project.scripts]
"scifireaders_mcp.exe" = "SciFiReaders.mcp.scifireaders_mcp:main"
"scifireaders_mcp" = "SciFiReaders.mcp.scifireaders_mcp:main"
"scifireaders-mcp" = "SciFiReaders.mcp.scifireaders_mcp:main"

[project.urls]
Homepage = "https://pycroscopy.github.io/SciFiReaders/about.html"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
exclude = ["*.tests", "*.tests.*", "tests.*", "tests"]

[tool.setuptools.dynamic]
version = { attr = "SciFiReaders.__version__.version" }
